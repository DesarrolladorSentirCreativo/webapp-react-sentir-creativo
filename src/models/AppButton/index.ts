export enum AppButtonColor {
  primary = 'primary',
  secondary = 'secondary',
  error = 'error',
  warning = 'warning',
  inherit = 'inherit',
  info = 'info',
}
export enum AppButtonVariant {
  text = 'text',
  outlined = 'outlined',
  contained = 'contained',
}

export enum AppButtonType {
  submit = 'submit',
  reset = 'reset',
  button = 'button',
}
