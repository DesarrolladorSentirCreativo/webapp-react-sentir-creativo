import HouseIcon from '@mui/icons-material/House'

import { type RouteType } from '../models'

const menu: RouteType[] = [
  {
    title: 'Home',
    icon: <HouseIcon/>,
    path: '/home'
  }
]

export { menu }
